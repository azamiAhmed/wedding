# Database (Neon via Vercel Postgres)
DATABASE_URL=postgresql://user:password@ep-XXXXX.neon.tech/wedding?sslmode=require

# Admin Authentication (bcrypt hash — generate with: node -e "require('bcryptjs').hash('yourpassword', 10).then(console.log)")
ADMIN_PASSWORD=$2b$10$XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
